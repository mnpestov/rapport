-- Новое значение enum Permission (BROWSER_ACCESS_PLAN.md §3.5, §10 миграция 3).
--
-- ОТДЕЛЬНЫЙ ФАЙЛ, и в нём ТОЛЬКО ADD VALUE — ничего больше сюда добавлять
-- нельзя. Postgres не даёт использовать новое значение enum в той же
-- транзакции, где оно добавлено, а Prisma оборачивает каждый файл миграции
-- в транзакцию. Любой INSERT/UPDATE со значением 'WEB_ACCESS' (например
-- массовая выдача на публичный запуск) обязан идти СЛЕДУЮЩИМ файлом.
ALTER TYPE "Permission" ADD VALUE 'WEB_ACCESS';
